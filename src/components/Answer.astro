---
export interface Props { label?: string; question?: string }
const { label = 'Réponse directe', question } = Astro.props;
---
<div class="answer">
  <span class="answer__label">{question ?? label}</span>
  <slot />
</div>
